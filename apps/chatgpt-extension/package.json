{
  "name": "@codex-context-bridge/chatgpt-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "clean": "node -e \"require('fs').rmSync('dist',{recursive:true,force:true})\"",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@codex-context-bridge/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.0",
    "vite": "^8.0.0"
  }
}
