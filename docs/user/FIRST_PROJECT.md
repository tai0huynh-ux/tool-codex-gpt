# First Project

1. Open the desktop app and register the repository root.
2. Review the normalized remote, canonical root, marker, and generated fingerprint.
3. Add aliases if the project has common alternate names.
4. Preview ChatGPT project/conversation evidence and explicitly confirm ambiguous mappings.
5. Register an existing Codex thread or select the new-thread workflow.

Confidence at or above `0.85` can be selected automatically. Scores from `0.60` through `0.84` require confirmation. Lower confidence blocks transfer. Equal-confidence candidates remain ambiguous until selected by the user.
